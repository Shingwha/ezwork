# Normalise text files to LF in the repo, check out as-is locally.
# Keeps diffs clean across Windows/macOS/Linux contributors.
* text=auto eol=lf

# Binary files — never touch
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
